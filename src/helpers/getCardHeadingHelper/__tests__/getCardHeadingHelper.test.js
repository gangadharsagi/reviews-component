import { getCardHeadingHelper } from '../getCardHeadingHelper';

describe('getCardHeadingHelper', () => {
  it('should ', () => {
    const expected = getCardHeadingHelper();
    const result = true;
    expect(expected).toEqual(result);
  });
});
