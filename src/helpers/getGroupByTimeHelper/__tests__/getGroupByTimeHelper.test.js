import { getGroupByTimeHelper } from '../getGroupByTimeHelper';

describe('getGroupByTimeHelper', () => {
  it('should ', () => {
    const expected = getGroupByTimeHelper();
    const result = true;
    expect(expected).toEqual(result);
  });
});
