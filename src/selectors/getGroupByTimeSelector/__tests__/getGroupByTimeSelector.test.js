import { getGetGroupByTime, getGroupByTimeSelector } from '../getGroupByTimeSelector';

describe('getGetGroupByTime', () => {
  it('should select data from the store state', () => {
    const mockState = {
      mockParam: {
        mockData: 'mockString',
      },
    };
    const expected = getGetGroupByTime(mockState);
    const result = 'mockString';
    expect(expected).toEqual(result);
  });
  it('should provide a default value if there is no value in the store', () => {
    const mockState = {
      mockParam: {
        mockData: 'mockString',
      },
    };
    const expected = getGetGroupByTime(mockState);
    const result = '';
    expect(expected).toEqual(result);
  });
});

describe('getGroupByTimeSelector', () => {
  it('should select data from the store state', () => {
    const mockState = {
      mockParam: {
        mockData: 'mockString',
      },
    };
    const expected = getGroupByTimeSelector(mockState);
    const result = 'mockString';
    expect(expected).toEqual(result);
  });
  it('should provide a default value if there is no value in the store', () => {
    const mockState = {
      mockParam: {
        mockData: 'mockString',
      },
    };
    const expected = getGroupByTimeSelector(mockState);
    const result = '';
    expect(expected).toEqual(result);
  });
});
